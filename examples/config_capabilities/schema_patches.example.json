{
  "schema_patches": [
    {
      "id": "add_output_format",
      "op": "add_field",
      "target": "/output",
      "name": "format",
      "schema": {
        "type": "string",
        "default": "json"
      }
    }
  ]
}
